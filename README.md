# swarmkhazad

A local, goal-driven swarm: SwarmForge's runtime, the `.claude` goal contract, and khazad's goal judge, in one Babashka repo.

Runtime home is `~/.swarmkhazad/`; one folder per task under `tasks/<task-id>/`.
